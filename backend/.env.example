# Backend Environment Variables for Blockchain Integration

# ===== CONTRACT ADDRESSES =====
# Address of the deployed TicketNFT contract
CONTRACT_ADDRESS=

# Address of the deployed TicketMarketplace contract (if using atomic purchase)
MARKETPLACE_ADDRESS=

# ===== WALLET & RPC =====
# Private key of the wallet that will mint NFTs (must be the owner of TicketNFT or authorized minter)
# WARNING: Never commit this file with real private keys! Keep .env in .gitignore
PRIVATE_KEY=

# RPC URL for connecting to the blockchain
# Examples:
# - Local Hardhat: http://localhost:8545
# - Sepolia Testnet: https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
# - Or use Alchemy: https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
RPC_URL=http://localhost:8545

# ===== SERVER CONFIGURATION =====
# Base URL for serving metadata files (defaults to http://localhost:4000)
BASE_URL=http://localhost:4000

# Port for the backend server (defaults to 4000)
PORT=4000

# Frontend origins allowed for CORS (comma-separated)
FRONTEND_ORIGIN=http://localhost:8080,http://localhost:8081
